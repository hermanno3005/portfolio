{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Terminal-style portfolio — a fake zsh session in the browser. The site itself has no build step and no runtime dependencies; everything here is test tooling only.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hermanno3005/portfolio.git"
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "vitest": "^4.1.10"
  }
}
